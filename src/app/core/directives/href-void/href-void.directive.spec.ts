import { inject, TestBed } from '@angular/core/testing';
import { HrefVoidDirective } from './href-void.directive';

describe('Href Void Directive', () => {
  beforeEach(() => TestBed.configureTestingModule({
    declarations: [
      HrefVoidDirective
    ]}));

});
