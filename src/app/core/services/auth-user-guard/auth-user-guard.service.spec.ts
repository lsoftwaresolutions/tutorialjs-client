import { inject, TestBed } from '@angular/core/testing';
import { AuthUserGuardService } from './auth-user-guard.service';

describe('Auth User Guard', () => {
  beforeEach(() => TestBed.configureTestingModule({
    providers: [
      AuthUserGuardService
    ]}));

});
